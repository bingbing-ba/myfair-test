import styled from '@emotion/styled';

const Header = styled.div`
  font-size: 56px;
  font-weight: 700;
  line-height: 72px;
`;

const TodoUserListHeader = () => {
  return <Header>To Do List</Header>;
};

export default TodoUserListHeader;
